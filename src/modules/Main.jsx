import { Text, View } from "react-native";
import AppBar from "../components/AppBar";

export const Main = () => {
  return (
    <View styles={{ flex: 1 }}>
      <AppBar />
      <Text>Open up App.js to start hgola working on your app!</Text>
    </View>
  );
};
